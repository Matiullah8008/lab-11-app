{
  "name": "aws-deployment-test-app",
  "version": "1.0.0",
  "description": "A simple Node.js web app to test AWS deployments.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
